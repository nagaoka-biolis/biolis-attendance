# BiOLiS 勤怠管理 — 開発ロードマップ

**ゴール:** まず自分のクリニックで実際に使い始める（実運用）

---

## Phase 1 — 実運用に必須（最優先・急ぐ）

- [ ] **1. クリニックの実際の位置を設定**
  現在 GPS 座標が仮値（東京駅 35.6895, 139.6917 / 半径300m）。
  実際のクリニック座標に変更しないと、院内打刻でも「クリニック外（要確認）」になる。
  → 必要: クリニックの住所 or 緯度経度

- [ ] **2. 休憩開始 / 休憩終了ボタン（追加機能）**
  打刻種別に `break_start` / `break_end` を追加。
  勤務時間の計算から休憩時間を差し引く。スタッフ画面に2ボタン追加。

- [ ] **3. 管理者へのメッセージ / コメント機能（追加機能）**
  打刻ミス等があったとき、スタッフが管理者へひと言送れる。
  例: 打刻に紐づくコメントボタン。新テーブル `notes`（または `messages`）を追加し、
  管理者ダッシュボードで未読/一覧表示。

- [ ] **4. 打刻ミスの修正機能**
  管理者が打刻時刻の編集・削除をできるようにする。

- [ ] **5. スタッフのアカウント登録**
  管理者画面からスタッフ追加（または安全な登録手順の整備）。

## Phase 2 — 運用を固める

- [ ] 給与計算用データの出力（CSV / Excel エクスポート）
- [ ] セキュリティ整備（RLS ポリシー確認、service_role キーのローテーション）
- [ ] 独自ドメイン設定（任意）

---

## 現状（実装済み）

- ログイン（メール + パスワード）
- スタッフ: 出勤/退勤打刻（GPS位置チェック・半径300m）、本日の勤務時間集計
- 管理者: 月次サマリー、日次ログ、スタッフ名/月での絞り込み
- DB: `profiles`(id, name, role, created_at) / `attendance`(id, user_id, type, timestamp, latitude, longitude, is_valid)
